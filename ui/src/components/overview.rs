use yew::prelude::*;
use yew_router::prelude::*;

use crate::{route::Route};

#[function_component]
pub fn Overview() -> Html {

    use_effect(move || {
        
        
        
        || {}
    });

    html! {
        <section class="section">{"hi"}</section>
    }
}